# --------------------------------------------------------------------------
# Agent Runtime -- Environment Variables
# Copy this file to .env and fill in the values for local development.
# --------------------------------------------------------------------------

# Server
PORT=8081
NODE_ENV=development

# Redis (Cloud Memorystore or local)
# Used for pub/sub commands from the control plane and general caching.
REDIS_URL=redis://localhost:6379

# Supabase (if the runtime needs direct DB access for agent configs)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# GCP (only needed when running outside Cloud Run)
GCP_PROJECT=your-gcp-project-id
GCP_REGION=us-central1
ENVIRONMENT=dev
