# --------------------------------------------------------------------------
# Protocols Service (MCP + A2A) -- Environment Variables
# Copy this file to .env and fill in the values for local development.
# --------------------------------------------------------------------------

# Server
PORT=8082
NODE_ENV=development

# Base URL for this service (used in A2A AgentCard discovery URLs)
BASE_URL=http://localhost:8082

# Internal service URLs
CONTROL_PLANE_URL=http://localhost:8080
AGENT_RUNTIME_URL=http://localhost:8081

# Service-to-service API key for A2A executor -> control plane calls.
# Create this key in the api_keys table and set the raw key here.
# Not required for local development if auth is disabled.
INTERNAL_API_KEY=

# Environment
ENVIRONMENT=dev
